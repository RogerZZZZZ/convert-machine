import {
    isObj,
    isFun,
    isStr,
} from 'LIBS/type'

import { has } from 'LIBS/helpers'

export const matchObject = function (
    data,
    obj
) {

}

const match = {
    parse: (data, keys) => {

    },
}

export default match